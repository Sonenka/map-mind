
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Question {
  id       String   @id @default(uuid())
  type     String
  question String?
  image    String?
  options  Json
  correct  String
}